# Secrets & environment — NEVER commit real keys.
.env

# Virtualenv
.venv/

# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Provided candidate pool — large/synthetic. Keep the dir (data/.gitkeep),
# ignore the data itself. README documents how to obtain it.
/data/*.jsonl

# Rank-time output — regenerated by `python src/rank.py`, not source.
/submission.csv

# Resumable embedding checkpoints (precompute 01) — transient; removed on success.
/artifacts/.embed_ckpt/

# OS / editor noise
.DS_Store
Thumbs.db
