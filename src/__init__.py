"""redrob-ranker source package.

Shared, importable modules used by both the offline precompute scripts and the
rank-time runtime. Submodules with a numeric prefix (``src/precompute/01_*.py``)
are run as scripts, not imported.
"""
