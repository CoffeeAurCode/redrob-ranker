# Mirror of the submission-portal metadata. Keep honest and in sync with reality.
# Fill every TODO before submitting (Session 12).

team:
  name: TODO            # team / handle as registered on the portal
  members:
    - name: Pranav Raj
      email: curiosus.steel@gmail.com
      role: TODO        # e.g. lead / engineer

submission:
  github_url: TODO      # public URL of THIS repo (the inner redrob-ranker repo)
  sandbox_link: https://huggingface.co/spaces/CoffeeAurCode/redrob-ranker-sandbox  # live Gradio Space (direct app: https://coffeeaurcode-redrob-ranker-sandbox.hf.space)
  reproduce_command: "python src/rank.py --candidates ./data/candidates.jsonl --out ./submission.csv"

# Declared honestly — which AI tools were used and how (graded for transparency).
ai_tools:
  - name: Gemini 2.5 Flash (Google AI Studio)
    used_for: Offline structured signal extraction + JD rubric + reasoning generation.
    where: src/precompute/* only — never inside rank.py.
  - name: Claude Code
    used_for: Scaffolding, code authoring, and review assistance.

# Compute environment for the reproduce step (must fit the Stage-3 budget).
compute_env:
  python_version: "3.11"
  cpu_only: true
  network: false
  max_runtime_minutes: 5
  max_memory_gb: 16
